<!DOCTYPE html>
<html>
<%- include("../partials/head") %>
<%- include("../partials/menu") %>
<head>
    <title>Nova Arrecadação</title>
    <meta charset="UTF-8">
</head>
<body>
<<<<<<< HEAD
    <h2>Nova Arrecadação</h2>
    
    <form action="/nova-arrecadacao" method="POST">
        <div>
            <label>Selecione uma turma:</label><br>
            <select name="id_turma" required>
                <option value="">Escolha uma turma</option>
                <% turmas.forEach(function(turma) { %>
                <option value="<%= turma.id_turma %>">
                    <%= turma.sigla %> - <%= turma.docente %>
                </option>
                <% }); %>
            </select>
        </div>
        
        <br>
        
        <div>
            <label>Selecione o Item:</label><br>
            <select name="id_Item" required>
                <option value="">Escolha um item</option>
                <% if (pontuacoes && pontuacoes.length > 0) { %>
                    <% pontuacoes.forEach(function(item) { %>
                    <option value="<%= item.id %>">
                        <%= item.Descricao %> (<%= item.pontos %> pontos)
                    </option>
                    <% }); %>
                <% } else { %>
                    <option value="">Nenhum item cadastrado</option>
                <% } %>
            </select>
        </div>
        
        <br>
        
        <div>
            <label>Selecione a quantidade:</label><br>
            <input type="number" name="qtd" min="1" value="1" required>
        </div>
        
        <br>
        
        <button type="submit">Enviar</button>
    </form>
=======
 
    <br><br>
  <h1 class="h1arrec">Nova Arrecadação</h1>
  <br>

    <form action="/nova-arrecadacao" method="post" class="envio" style="background-color: #d0bdf4">

    <label for="turma">⠀Selecione uma turma:⠀⠀⠀</label>
    <select name="id_turma" id="id_turma" style="width: 200px;">
      <option value="">[Escolha]</option>
      <% let i = 0; for (i = 0 ; i< turmas.length; i++) {%>
      <option value="<%= turmas[i].id_turma %>"><%= turmas[i].sigla %> - [<%= turmas[i].docente %>]</option>
      <% } %>
    </select>
    <br>

    <br>&nbsp;  

    <label for="itens">Selecione o item:ㅤ⠀⠀⠀⠀</label>
    <select name="id_roupa" id="id_roupa" style="width: 200px;">
      <option value="">[Escolha]</option>
      <% for (let i = 0; i < pontuacoes.length; i++) { %>
      <option value="<%= pontuacoes[i].id %>">
        <%= pontuacoes[i].Descricao %> - <%= pontuacoes[i].pontos %> pontos
      </option>
      <% } %>
    </select>
<br>
    <br>&nbsp;

    <label for="quantidade">Selecione a quantidade:⠀</label>
    <select name="qtd" id="qtd" style="width: 200px;">
      <option value="">[Escolha]</option>
      <% for (let i = 1; i <= 15; i++) { %>
      <option value="<%= i %>"><%= i %></option>
      <% } %>
    </select>
    <br><br>

    <button type="submit" class="novarrec">Enviar</button>

  </form>
</center>

<br><br>
<%- include("../partials/footer") %>
>>>>>>> 9eb1b25dcf2f8e94cfd76e8815fa314449befd2a
</body>
<%- include("../partials/footer") %>
</html>